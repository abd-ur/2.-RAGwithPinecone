## Run streamlit app for RAG demo:
1. Download app.py, requirements.txt and variant.json locally
2. Run
```bash
pip install -r requirements.txt
```
3. Run
```bash
streamlit run app.py
```


*Might take sometime to download BioBERT and biogpt for running locally*
